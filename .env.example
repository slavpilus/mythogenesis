# Copy to .env and fill in. The app runs without these using a deterministic
# local fallback, but the Hugging Face Inference API backend needs a token.

# Hugging Face token (https://huggingface.co/settings/tokens)
HF_TOKEN=

# Optional: override the default model id once chosen (must be <=32B total params)
# MODEL_ID=
